<!--================================================================
                        해야할 일

1. md사용해 보기 (online markdown검색) (완료)
2. js책 정독
3. w3school에서 table태그 읽어보기 
==================================================================-->

<!--=================================================================
                        학습내용

HTML
    0. HTML은 태그로 이루어져 있다 (XML)
    1. 태그는 열림이 있으면 닫힘이 있다.
    2. 계층관계다. (부모자식 관계)
    3. 태그 안에는 attribute(속성) = "value(값)"이 존재한다.
        3-1. 태그마다 가지고 있는 속성이 다 다르다.

    ****. 자식태그를 작성할 때 2칸/4칸 들여쓰기한다. 일관성!!!! (코드컨베션)
    ****. 항상 개념을 파악하고 w3school 읽기를 생활화 하자
    ****. 이해가 안되면 포기하지말고 고민고민고민

JavaScript
    1. 모든 객체에는 생명주기(life cycle)이 존재한다.
    2. managed language(c, c++), unmanged language(js, java, python) - 가비지 컬렉터
    3. 컴파일언어(java, c#), 인터프리터(js)
    4. stack: 선입후출, queue: 선입선출
    5. 호이스팅(hoisting) 끌어올린다.
    6. 함수는 객체다. (자바스크립트에서 함수는 *일급객체* 라 칭한다.)
==================================================================-->


